extends layout/main
include mixin/videoBlock

block content
    .search__header
        h3 Searching for: #{searchingBy}
    .search__videos
        each item in sampleVideos
            +videoBlock({
                videoFile: item.videoFile,
                title: item.title,
                views: item.views,
            })