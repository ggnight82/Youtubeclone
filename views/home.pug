extends layout/main
include mixin/videoBlock

block content
    .videos
        each item in sampleVideos
            +videoBlock({
                videoFile: item.videoFile,
                title: item.title,
                views: item.views,
            }) 